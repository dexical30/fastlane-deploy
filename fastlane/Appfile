# fastlane/Appfile

app_identifier(ENV["APP_IDENTIFIER"])
apple_id(ENV["FASTLANE_APPLE_ID"])
team_id(ENV["APPLE_TEAM_ID"])
# itc_team_id(ENV["FASTLANE_ITC_TEAM_ID"])

# Android
package_name(ENV["APP_IDENTIFIER"])
json_key_file(File.expand_path(File.join(__dir__, "..", "credentials", "playstore.json")))
